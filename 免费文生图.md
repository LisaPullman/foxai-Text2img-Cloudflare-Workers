# 免费文生图：原理介绍

> 本文解释本项目「免费在线文生图」的完整原理：免费额度从哪里来、文生图模型如何把文字变成图片、整个系统如何在无服务器的情况下运行。
> 模型参数与定价信息核对于 2026-09（Cloudflare 官方文档），最新数据以 [Workers AI 定价页](https://developers.cloudflare.com/workers-ai/platform/pricing/) 与各[模型详情页](https://developers.cloudflare.com/workers-ai/models/)为准。

---

## 目录

1. [为什么能"免费"](#1-为什么能免费)
2. [计费原理：神经元与伙伴模型](#2-计费原理神经元与伙伴模型)
3. [文生图模型原理](#3-文生图模型原理)
4. [本项目架构](#4-本项目架构)
5. [关键参数原理](#5-关键参数原理)
6. [可用模型清单](#6-可用模型清单)
7. [常见问题](#7-常见问题)

---

## 1. 为什么能"免费"

本项目的"免费"来自 **Cloudflare Workers AI 的免费额度**：

- Cloudflare Workers 免费版每天提供 **10,000 个神经元（Neurons）** 的 Workers AI 推理额度，次日自动重置；
- 每次文生图消耗的神经元与「模型 × 分辨率 × 步数」相关。以默认模型 FLUX.1 [schnell]（4 步、1024×1024）为例，单张消耗极小，日常个人使用很难用完每日额度；
- SDXL Base、SDXL-Lightning 等官方 Beta 模型按步计费为 **$0.00/步**，实际只占神经元额度；
- Worker 本体（网页托管 + API 转发）每天 10 万次请求免费，对于个人图床级的使用完全够用。

一句话：**Cloudflare 把 GPU 推理按"神经元"计量并每日赠送一批，本项目只是把这份额度包装成了一个网页。**

## 2. 计费原理：神经元与伙伴模型

Workers AI 的模型分两类，计费方式不同：

| 类型 | 计费 | 说明 |
| --- | --- | --- |
| Cloudflare 托管模型（SDXL 系列、FLUX.1 等） | 按神经元 | 免费额度（神经元/日）优先抵扣，超出后按量付费 |
| Partner 伙伴模型（FLUX.2 系列、Leonardo 系列） | 按用量美元计价 | 不占（或少量折算）免费神经元，需要账户启用付款方式，未绑定支付方式时调用会直接报错 |

神经元是 Cloudflare 的统一算力单位（大致对应 GPU 计算时间），文本、图像、语音模型都折算成同一单位，因此免费额度可以在不同模型间通用。

## 3. 文生图模型原理

本项目接入的模型分为三代技术，理解它们的差异有助于选型：

### 3.1 潜空间扩散模型（Stable Diffusion XL / DreamShaper / Lightning）

核心思想是「**从噪声中一步步去噪**」：

1. **文本编码**：CLIP/text-encoder 把提示词编码为语义向量；
2. **加噪训练**：训练时对图片逐步添加高斯噪声，让 U-Net 学会预测"这一步加了什么噪声"；
3. **潜空间去噪**：生成时从一张纯噪声图开始，U-Net 在压缩的潜空间（约 64×64）中按提示词方向迭代去噪 `num_steps` 次；
4. **解码放大**：VAE 解码器把潜空间还原为 1024×1024 像素图。

在潜空间而非像素空间操作，是 SD 系列比早期扩散模型快一个数量级的关键。

- **DreamShaper 8 LCM**：在 SD 基础上用 LCM（潜在一致性模型）蒸馏，4~8 步即可出图；
- **SDXL-Lightning**：字节跳动用对抗蒸馏加速，官方定位"几步出 1024px 高质量图"，实测 2~8 步效果最佳。

### 3.2 整流流 Transformer（FLUX 系列）

FLUX 放弃 U-Net，改用 **Transformer 主干 + 整流流（Rectified Flow）**：

- 把"噪声 → 图片"看作一条直线路径，模型学习把任意中间状态"拉直"推向目标；
- 并行注意力设计，对文字语义、画面文字渲染、手部结构等细节明显强于同代 SD；
- **FLUX.1 [schnell]**：schnell（德语"快"）为 4 步蒸馏版——官方推荐仅 4 步即可出图，速度快且免费额度消耗低，是本项目默认模型；
- **FLUX.2 系列（klein 4B/9B、dev）**：2026 年新一代，统一了生成与编辑，质量进一步增强。注意它们在 Workers AI 上以 **multipart 表单**传输参数，且属于付费伙伴模型。

### 3.3 商业闭源模型（Leonardo Lucid Origin / Phoenix 1.0）

Leonardo.Ai 的闭源商用模型，走 Cloudflare 的推理通道：

- **Phoenix 1.0**：提示词遵循度与画面内文字连贯性出色；
- **Lucid Origin**：响应长提示词能力强，支持最高 2500px 的全高清输出。

两者均为付费伙伴模型，适合对质量有要求且已配置付款方式的部署。

## 4. 本项目架构

整个服务只有两个文件（`src/worker.js` + `src/index.html`），没有服务器、没有数据库、没有构建步骤：

```
浏览器                    Cloudflare 边缘节点                 GPU 推理集群
┌─────────────┐   HTTPS   ┌──────────────────────┐   binding  ┌───────────┐
│  index.html │ ────────► │  Worker (V8 isolate) │ ─────────► │ Workers AI│
│  原生 JS UI  │ ◄──────── │  · 托管页面           │ ◄───────── │  文生图模型 │
└─────────────┘  图像/PNG  │  · /api/models 能力表  │   图像字节  └───────────┘
                          │  · POST / 校验+调用 AI │
                          └──────────────────────┘
```

关键机制：

1. **Workers AI 绑定**：Worker 通过 `env.AI.run(modelKey, inputs)` 直呼模型，无需 API Key、无需公网转发，账号内网直达推理集群；
2. **能力自描述**：`worker.js` 里的 `AVAILABLE_MODELS` 同时是服务端的校验规则（钳制尺寸/步数/引导系数、按模型拼装参数）和前端的 UI 说明书（`/api/models` 下发后，页面自动显隐「反向提示词 / 尺寸 / 引导系数 / 种子 / 画面比例」控件并调整滑块范围）——一份配置，两端生效；
3. **两种传输形态**：SD/Leonardo 系列直接传 JSON、返回图像流；FLUX.2 系列参数序列化为 multipart FormData 传输、返回 base64 JSON，Worker 统一转换成浏览器可直接显示的二进制响应；
4. **边缘执行**：Worker 代码在全球 300+ 城市节点就近运行，页面加载与请求转发都是毫秒级；图像生成耗时（秒级）主要取决于 GPU 排队与模型步数；
5. **可选访问密码**：在 `PASSWORDS` 数组填入密码后，所有生成请求必须携带正确密码，适合私有部署防蹭额度。

## 5. 关键参数原理

| 参数 | 原理 | 实用建议 |
| --- | --- | --- |
| 迭代步数 steps / num_steps | 去噪迭代次数，步数越多细节越充分 | schnell 固定 4 步；Lightning/LCM 2~8 步；SDXL 20 步；超过模型最佳区间只会更慢不会更好 |
| 引导系数 guidance | CFG 强度：去噪方向被提示词"拉"得多紧 | SDXL 常用 7.5；蒸馏模型宜低（1~2）；过高会颜色过饱和、构图僵化 |
| 随机种子 seed | 初始噪声图的随机源；同种子+同参数=可复现 | 想微调提示词对比效果时固定种子，其余场景留空随机 |
| 反向提示词 negative_prompt | 告诉模型"远离"的语义方向 | 常用 `lowres, bad anatomy, blurry`；FLUX 系不支持（架构上去除了 CFG 依赖） |
| 尺寸 width/height | 潜空间分辨率对应关系，需为 8 的倍数 | 贴近模型原生分辨率（1024×1024 / 1120×1120）质量最佳，过大易重复主体 |
| 画面比例 aspect_ratio | FLUX.2 [dev] 用比例替代像素 | 按构图需要选 16:9（横幅）、9:16（手机壁纸）等 |

## 6. 可用模型清单

| 模型 | 技术路线 | 计费 | 单价（2026-09 核对） | 特点 |
| --- | --- | --- | --- | --- |
| FLUX.1 [schnell] | 整流流 Transformer | 神经元 | $0.000053/512²tile + $0.00011/步 | 4 步出图，**默认推荐** |
| SDXL Base 1.0 | 潜空间扩散 | 神经元 | $0.00/步（Beta） | 参数可调空间最大 |
| DreamShaper 8 LCM | SD + LCM 蒸馏 | 神经元 | 见[官方页面](https://developers.cloudflare.com/workers-ai/models/dreamshaper-8-lcm/) | 真实感增强，少步数 |
| SDXL Lightning | SD + 对抗蒸馏 | 神经元 | $0.00/步（Beta） | 极快，2~8 步 |
| FLUX.2 [klein] 4B | 整流流蒸馏 | 付费伙伴 | $0.000287/输出 512²tile | 超快，实时预览级 |
| FLUX.2 [klein] 9B | 整流流蒸馏 | 付费伙伴 | $0.015/首 MP + $0.002/续 MP | 同上，质量更高 |
| FLUX.2 [dev] | 整流流 | 付费伙伴 | $0.00021/输入tile·步 + $0.00041/输出tile·步 | 高写实，比例出图 |
| Leonardo Lucid Origin | 闭源商用 | 付费伙伴 | $0.007/512²tile + $0.00013/步 | 长提示词、2500px |
| Leonardo Phoenix 1.0 | 闭源商用 | 付费伙伴 | $0.0058/512²tile + $0.00011/步 | 提示词遵循、文字渲染 |

> 另有 `stable-diffusion-v1-5-img2img / inpainting` 两个图生图/局部重绘模型，因需要输入参考图，未纳入本项目的纯文生图 UI。

## 7. 常见问题

**Q：生成要多久？**
3~20 秒不等，主要取决于模型（蒸馏模型快）、步数、分辨率以及当时 GPU 排队情况。

**Q：免费额度用完会怎样？**
当天剩余时间内的生成请求会返回额度错误，次日自动重置。为部署配置 `PASSWORDS` 可防止他人消耗你的额度。

**Q：选了付费伙伴模型但没绑卡？**
调用会返回计费类错误（页面会显示服务端错误信息）。换回 FLUX.1 [schnell] 或 SDXL 系列即可正常使用。

**Q：为什么同一提示词每次结果不同？**
未固定随机种子时每次初始噪声不同。想复现就把「随机种子」固定为同一个值（部分模型如 FLUX.1 [schnell] 的接口不接受种子参数，界面会自动隐藏该选项）。

**Q：图片保存在哪里？**
哪里都不保存——本项目不落盘。图像在 Worker 内存中生成后直接流回浏览器，刷新即逝，下载才留存在本地。如需历史记录，可自行扩展 KV/R2 存储。

---

*本文档属于 [foxai-Text2img-Cloudflare-Workers](https://github.com/LisaPullman/foxai-Text2img-Cloudflare-Workers) 项目。*
